<Window x:Class="UniqueFileMover.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переміщення оригінальних файлів" Height="350" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Джерело:" Width="80"/>
            <TextBox x:Name="SourcePathBox" Width="400" Margin="5,0,0,0"/>
            <Button Content="..." Width="30" Margin="5,0,0,0" Click="BrowseSource_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Приймач:" Width="80"/>
            <TextBox x:Name="DestinationPathBox" Width="400" Margin="5,0,0,0"/>
            <Button Content="..." Width="30" Margin="5,0,0,0" Click="BrowseDestination_Click"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Перемістити оригінальні файли" Height="35" 
                Click="MoveFiles_Click"/>

        <TextBox Grid.Row="3" x:Name="OutputBox" Margin="0,10,0,0" 
                 TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
